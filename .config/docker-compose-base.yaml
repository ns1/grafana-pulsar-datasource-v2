services:
  grafana:
    user: root
    container_name: 'grafana-pulsar-datasource-v2'

    build:
      context: .
      args:
        grafana_image: ${GRAFANA_IMAGE:-grafana-enterprise}
        grafana_version: ${GRAFANA_VERSION:-12.0.2}
        development: ${DEVELOPMENT:-false}
        anonymous_auth_enabled: ${ANONYMOUS_AUTH_ENABLED:-true}
    ports:
      - 3000:3000/tcp
      - 2345:2345/tcp # delve
    security_opt:
      - 'apparmor:unconfined'
      - 'seccomp:unconfined'
    cap_add:
      - SYS_PTRACE
    volumes:
      - ../dist:/var/lib/grafana/plugins/grafana-pulsar-datasource-v2
      - ../provisioning:/etc/grafana/provisioning
      - ..:/root/grafana-pulsar-datasource-v2

    environment:
      NODE_ENV: development
      GF_LOG_FILTERS: plugin.grafana-pulsar-datasource-v2:debug
      GF_LOG_LEVEL: debug
      GF_DATAPROXY_LOGGING: 1
      GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS: grafana-pulsar-datasource-v2
