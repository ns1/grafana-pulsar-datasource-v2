apiVersion: 1

datasources:
  - name: 'NS1 pulsar Metrics'
    type: 'grafana-pulsar-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      path: '/resources'
    secureJsonData:
      apiKey: 'api-key'
